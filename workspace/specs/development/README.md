# Development Specs

Specifications with active implementation work that has not yet been
integrated into `development`, this repository's configured test target.

Required path:

```text
development/<primary-feature>/<spec>.md
```

Move a development spec to `test/<primary-feature>/` only after recording the
confirmed integration event in the spec and root catalog rationale.
