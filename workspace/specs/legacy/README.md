# Legacy Specs

Historical current-project specs preserved during workspace-docs adoption.

These paths are not part of the active `backlog -> development -> test -> done`
lifecycle and are not catalogued as non-legacy rows.
